# EasyGoldGlitch Luxury Landing (Next.js)

## Run locally

```bash
npm install
npm run dev
```

Open: http://localhost:3000

## Real gold price source

- Server route: `GET /api/gold`
- Upstream data: Yahoo Finance futures symbol `GC=F` (used as real-time gold proxy for XAUUSD).
